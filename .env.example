# Drizzle
DATABASE_URL=local.db

# Live updates (ittysockets.com pub/sub). Pick a long random channel name —
# the channel is public, so it only ever carries content-free "changed" pings.
# Leave unset to disable realtime (e.g. in CI).
PUBLIC_REALTIME_CHANNEL=
