name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run check

      - name: Tests
        run: npm test

      - name: Build
        run: npm run build
        env:
          # SvelteKit's post-build analysis imports the server bundle, which
          # opens the database at import time. No queries run — an in-memory
          # DB satisfies it without touching disk.
          DATABASE_URL: ':memory:'
