node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# SQLite (database + journal/WAL sidecar files)
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Logs
npm-debug.log*
*.log

# Excel lock files (created while a spreadsheet is open)
~$*.xlsx

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# Project brief (kept locally, not part of the repo)
INSTRUCTIONS.md

# Deployment notes (kept locally, not part of the repo)
DEPLOYMENT.md
